generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  password  String
  company   String?
  role      String   @default("user")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  
  notifications  Notification[]
  chatSessions   ChatSession[]
  tutorials      Tutorial[]
  theme          Theme?
  
  @@map("users")
}

model PurchaseOrder {
  id                String   @id @default(cuid())
  date              String
  supplier          String
  orderNo           String   @map("order_no")
  refNo             String   @map("ref_no")
  dueDate           String   @map("due_date")
  branch            String
  requisitionType   String   @map("requisition_type")
  itemLedgerGroup   String   @map("item_ledger_group")
  item              String
  minQty            Float    @map("min_qty")
  maxQty            Float    @map("max_qty")
  unit              String
  rate              Float
  deliveryDate      String   @map("delivery_date")
  cgst              Float
  sgst              Float
  igst              Float
  vat               Float
  lastApprovedRate  Float    @map("last_approved_rate")
  lastSupplier      String   @map("last_supplier")
  broker            String
  totalAmount       Float    @map("total_amount")
  status            String
  deliveryType      String   @map("delivery_type")
  openPO            String   @map("open_po")
  openPONo          String   @map("open_po_no")
  uploadedAt        DateTime @default(now()) @map("uploaded_at")
  isApproved        Boolean  @default(false) @map("is_approved")
  approvalNotes     String?  @map("approval_notes")
  
  @@map("purchase_orders")
}

model PendingPO {
  id                String   @id @default(cuid())
  date              String
  supplier          String
  orderNo           String   @map("order_no")  // Remove @unique from here
  refNo             String   @map("ref_no")
  dueDate           String   @map("due_date")
  branch            String
  requisitionType   String   @map("requisition_type")
  itemLedgerGroup   String   @map("item_ledger_group")
  item              String
  minQty            Float    @map("min_qty")
  maxQty            Float    @map("max_qty")
  unit              String
  rate              Float
  lastApprovedRate  Float    @map("last_approved_rate")
  lastSupplier      String   @map("last_supplier")
  totalAmount       Float    @map("total_amount")
  status            String
  deliveryType      String   @map("delivery_type")
  
  // API specific fields
  approvalLevel     String   @map("approval_level")
  empId             String   @map("emp_id")
  
  // Metadata
  fetchedAt         DateTime @default(now()) @map("fetched_at")
  isProcessed       Boolean  @default(false) @map("is_processed")
  
  @@map("pending_pos")
  @@index([orderNo])
  @@index([status])
  @@index([isProcessed])
}

model ApprovalPO {
  id                String   @id @default(cuid())
  date              String
  supplier          String
  orderNo           String   @map("order_no")
  refNo             String   @map("ref_no")
  dueDate           String   @map("due_date")
  branch            String
  requisitionType   String   @map("requisition_type")
  itemLedgerGroup   String   @map("item_ledger_group")
  item              String
  minQty            Float    @map("min_qty")
  maxQty            Float    @map("max_qty")
  unit              String
  rate              Float
  deliveryDate      String   @map("delivery_date")
  cgst              Float
  sgst              Float
  igst              Float
  vat               Float
  lastApprovedRate  Float    @map("last_approved_rate")
  lastSupplier      String   @map("last_supplier")
  broker            String
  totalAmount       Float    @map("total_amount")
  status            String   @default("approved")
  deliveryType      String   @map("delivery_type")
  openPO            String   @map("open_po")
  openPONo          String   @map("open_po_no")
  
  // Approval metadata
  approvedAt        DateTime @default(now()) @map("approved_at")
  approvalNotes     String?  @map("approval_notes")
  
  @@map("approval_pos")
  @@index([orderNo])
  @@index([status])
  @@index([approvedAt])
}

model ChatSession {
  id        String   @id @default(cuid())
  userId    String   @map("user_id")
  title     String
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  
  user      User           @relation(fields: [userId], references: [id], onDelete: Cascade)
  messages  ChatMessage[]
  
  @@map("chat_sessions")
}

model ChatMessage {
  id            String      @id @default(cuid())
  sessionId     String      @map("session_id")
  role          String
  content       String      @db.Text
  timestamp     DateTime    @default(now())
  
  session       ChatSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  
  @@map("chat_messages")
}

model Notification {
  id        String   @id @default(cuid())
  userId    String   @map("user_id")
  type      String
  title     String
  message   String
  count     Int?
  severity  String
  read      Boolean  @default(false)
  link      String?
  createdAt DateTime @default(now()) @map("created_at")
  
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@map("notifications")
}

model Tutorial {
  id            String   @id @default(cuid())
  userId        String   @map("user_id")
  tutorialId    String   @map("tutorial_id")
  completed     Boolean  @default(true)
  completedAt   DateTime @default(now()) @map("completed_at")
  
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@unique([userId, tutorialId])
  @@map("tutorials")
}

model Theme {
  id        String   @id @default(cuid())
  userId    String   @unique @map("user_id")
  themeName String   @default("cyberpunk") @map("theme_name")
  updatedAt DateTime @updatedAt @map("updated_at")
  
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@map("themes")
}